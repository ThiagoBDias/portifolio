---
// Componente de FAQ para a página de contato
const faqs = [
  {
    question: "Quanto tempo leva para desenvolver um site completo?",
    answer: "O tempo de desenvolvimento varia de acordo com a complexidade do projeto. Um site institucional simples pode levar de 2 a 4 semanas, enquanto um e-commerce ou aplicação web mais complexa pode levar de 2 a 6 meses."
  },
  {
    question: "Quais tecnologias você utiliza nos seus projetos?",
    answer: "Trabalho principalmente com React, TypeScript, Node.js e bancos de dados SQL/NoSQL. Para cada projeto, escolho as tecnologias mais adequadas considerando fatores como escalabilidade, desempenho e necessidades específicas do cliente."
  },
  {
    question: "Você oferece manutenção após a entrega do projeto?",
    answer: "Sim! Ofereço planos de manutenção que incluem atualizações de segurança, correções de bugs e pequenas melhorias. Também estou disponível para implementar novas funcionalidades e expansões do projeto conforme necessário."
  },
  {
    question: "Como funciona o processo de desenvolvimento?",
    answer: "O processo começa com uma análise detalhada dos requisitos, seguida pela criação de wireframes e protótipos. Após aprovação do design, inicio o desenvolvimento com ciclos regulares de feedback. O projeto só é finalizado após testes completos e sua aprovação."
  }
]
---

<div class="py-12">
  <h2 class="text-2xl md:text-3xl font-bold text-white text-center mb-10">Perguntas Frequentes</h2>
  
  <div class="space-y-6 max-w-3xl mx-auto">
    {faqs.map((faq, index) => (
      <div class="faq-item bg-gray-800/40 border border-gray-700/50 rounded-lg overflow-hidden hover:border-primary-500/30 transition-all duration-300">
        <button class="faq-question w-full flex justify-between items-center p-5 text-left focus:outline-none">
          <span class="text-lg font-medium text-white">{faq.question}</span>
          <svg class="faq-icon w-5 h-5 text-primary-400 transform transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
          </svg>
        </button>
        <div class="faq-answer hidden px-5 pb-5">
          <p class="text-gray-400">{faq.answer}</p>
        </div>
      </div>
    ))}
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const faqItems = document.querySelectorAll('.faq-item');
    
    faqItems.forEach(item => {
      const question = item.querySelector('.faq-question');
      const answer = item.querySelector('.faq-answer');
      const icon = item.querySelector('.faq-icon');
      
      if (question && answer && icon) {
        question.addEventListener('click', () => {
          // Toggle current item
          answer.classList.toggle('hidden');
          icon.classList.toggle('rotate-180');
          
          // Close other items
          faqItems.forEach(otherItem => {
            if (otherItem !== item) {
              const otherAnswer = otherItem.querySelector('.faq-answer');
              const otherIcon = otherItem.querySelector('.faq-icon');
              
              if (otherAnswer) otherAnswer.classList.add('hidden');
              if (otherIcon) otherIcon.classList.remove('rotate-180');
            }
          });
        });
      }
    });
  });
</script>
