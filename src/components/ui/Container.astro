---
export interface Props { size?: 'sm' | 'md' | 'lg' | 'xl'; class?: string; }
const { size = 'lg', class: className = '' } = Astro.props;
const sizes = { sm: 'max-w-4xl', md: 'max-w-6xl', lg: 'max-w-7xl', xl: 'max-w-screen-2xl' };
---
<div class={`${sizes[size]} mx-auto px-4 sm:px-6 lg:px-8 ${className}`}><slot /></div>
